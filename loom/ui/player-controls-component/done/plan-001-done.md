---
type: done
id: pl_01KXKVQR28RWR6PF1STVZ3XEDX-done
title: Done — Extract shared PlayerControlsR + engine event bus
status: done
created: 2026-07-15
version: 6
tags: []
parent_id: pl_01KXKVQR28RWR6PF1STVZ3XEDX
requires_load: []
---
# Done — Extract shared PlayerControlsR + engine event bus

## Step 1 — Add a light multi-subscriber event API to ChordFlowPlayback: engine.on("stateChange"|"ready"|"soundFontsListed"|"beat"|"finished", cb), keeping the existing create({onBeat,…}) callbacks as sugar that register on the same buses.

Added a multi-subscriber event bus to `ChordFlowPlayback` (playback-component.js): a `listeners` map over `beat`/`stateChange`/`ready`/`finished`/`soundFontsListed`, an internal `emit()`, and a closure `on(event, handler)`. The create-time `onBeat`/`onStateChange`/`onReady`/`onFinished`/`onSoundFontsListed` callbacks are now seeded onto the same buses (back-compat sugar), replacing the old single-`cb` object. All former `cb.onX(...)` call sites now `emit(...)`. Exposed `on(event, handler)` on the returned handle so a consumer (page marker) and PlayerControlsR can each subscribe without the page forwarding events. JS syntax-checked; no residual `cb.` references.

## Step 2 — Create wwwroot/player-controls-component.js (window.ChordFlowPlayerControls): create(container, engine, opts) builds play/pause·stop·tempo·(soundfont)·(metronome)·(count-in)·(optional now-next), wires each to the engine handle, self-subscribes to stateChange/ready/soundFontsListed/finished, and wraps every handler in a log-not-die guard. Add its <script> to index.html.

Created `wwwroot/player-controls-component.js` (`window.ChordFlowPlayerControls`). `create(container, engine, opts)` builds a self-styled `<span class="cf-player-controls">` with play/pause · stop · tempo · (soundfont) · (metronome) · (count-in) · (optional Now/Next), wires each control straight to the engine handle (`engine.play/stop/setTempo/setSoundFont/setMetronome/setCountIn`), and **self-subscribes** to the engine bus (`ready`→enable transport, `stateChange`→play/pause label, `finished`→reset, `soundFontsListed`→fill picker). Every handler is wrapped in a `guard()` that `console.error`s instead of letting a throw be swallowed by the DOM dispatcher (IN6). Optional controls omitted when their opt is false; Now/Next rendered only when `onToggleNowNext` is supplied (IN5). Returns `{ el, setTempoValue, getTempo, dispose }`. Registered the `<script>` in index.html (after playback-component.js, before score-render/chord-sheets) and added `.cf-player-controls` CSS so it looks consistent mounted in ScoreR's strip or the chord-sheets page. JS syntax-checked.

## Step 3 — Split ScoreR.buildControls: mount PlayerControlsR for the player controls (play/stop/tempo/soundfont/metronome/count-in/now-next) and drop that wiring; keep ScoreControls (staff-profile, chord-names, diagrams, auto-layout, key/feel, scroll-mode, alphaTex debug panel + their syncToggle). ScoreR keeps owning the engine + cf-score-surface + getApi. Preserve ScoreR's public API and Practice behavior.

Recomposed ScoreR (score-render-component.js) onto PlayerControlsR:\n- create() now builds `pc = ChordFlowPlayerControls.create(null, engine, { onToggleNowNext: opts.onToggleNowNext ? (v)=>handle.toggleNowNext(v) : null })` and passes it into buildControls, which mounts `pc.el` at the head of the `.cf-controls` strip.\n- buildControls dropped play/stop/tempo, the metronome/count-in toggles, the soundfont picker, and the now-next toggle (all now owned by PlayerControlsR); it keeps scroll-mode, the two volume sliders, key/feel, staff-profile, and the display toggles.\n- Removed the now-dead helpers `reflectPlayState`, `setTransportEnabled`, `fillSoundFontPicker`, the builder fns `soundFontPicker`/`nowNextToggle`, and the unused `span` helper; trimmed `ui` to `{key,tripletFeel,staffProfile,toggles}` and added `let pc`.\n- Engine callbacks simplified: onStateChange just forwards to cb (label owned by pc); onReady/onSoundFontsListed dropped (pc self-subscribes). Tempo display delegated to pc: load()/seedTempo() call `pc.setTempoValue`, getTempo() reads `pc.getTempo()`.\n- Removed dead metronome/count-in plumbing: PLAYER_KIND set + the setOption PLAYER_KIND branch, and metronome/countIn from DEFAULT_OPTIONS + the header opts example.\n- ScoreR still owns the engine, cf-score-surface, getApi, scoreLoaded handling, staff-profile, and the debug panel (syncToggle retained for display toggles). Public API (view.play/stop/load/setTempo/getTempo/setOption/...) unchanged. JS syntax-checked; no dangling refs.

## Step 4 — Replace chord-sheets.js buildTransport's hand-rolled transport (play/stop/tempo/soundfont) with PlayerControlsR mounted on the page's own engine — gaining metronome + count-in. Keep ChordSheetControls (layout/notation/adornments/tone-labels/theme/export), marker-mode, and Show-tab. Do not pass onToggleNowNext (now-next boards deferred, EX1).

Recomposed the Chord Sheets page (chord-sheets.js) onto PlayerControlsR:\n- buildTransport no longer builds play/stop/tempo/soundfont — it now builds only the page-specific marker-mode select and Show-tab checkbox. Removed the `playBtn/stopBtn/tempoInput/soundFontSel` module vars (added `let pc`).\n- setupEngine creates the engine with just `onBeat` (marker) + `onFinished` (clear marker); the transport button/label/enable, tempo, and the soundfont picker are now owned by PlayerControlsR. After creating the engine it does `pc = ChordFlowPlayerControls.create(null, engine, {})` and `transportEl.insertBefore(pc.el, transportEl.firstChild)` so the transport leads the strip (order: pc, marker-mode, Show-tab). The page keeps owning the engine (C1); ChordSheetR untouched (C2); the soundfont picker still reflects the single global host-persisted choice (C3).\n- onHostMessage seeds tempo via `pc.setTempoValue(tempo)` on new content. No `onToggleNowNext` passed → no Now/Next toggle here (EX1, deferred).\n- Net gain: metronome + count-in now present on Chord Sheets (IN4). Build succeeds (0 errors); JS syntax-checked; no dangling refs.

## Step 5 — Dogfood both surfaces: Practice (ScoreR) transport/metronome/count-in/now-next/display toggles/debug panel unchanged; Chord Sheets play/stop/tempo/soundfont still work AND metronome + count-in now work, marker-mode/Show-tab/export unchanged and export still light-pinned. Use CHORDFLOW_DEVTOOLS to spot-check engine.metronomeVolume live.

Live dogfood confirmed by Rafa on both surfaces: Practice (ScoreR) transport/metronome/count-in/Now-Next/display toggles/debug panel behave as before, and Chord Sheets now has working metronome + count-in alongside the existing play/stop/tempo/soundfont, with marker-mode/Show-tab/export unchanged and the soundfont picker reflecting the same global choice. Static parity trace + `dotnet build` (0 errors) preceded the click-through. Parity (C4) holds.

## Step 6 — Update loom/refs/chordflow-architecture-reference.md with a diagram of the ScoreR/ChordSheetR-page → PlayerControlsR → ChordFlowPlayback composition and the engine event bus, plus prose for the new shared component and dependency edge.

Updated `loom/refs/chordflow-architecture-reference.md` (via loom_patch_doc, versioned): (1) extended the §7 "Playback engine seam" with the PlayerControlsR paragraph — the shared transport widget that binds to a ChordFlowPlayback handle (not owner), mounted by both ScoreR and the Chord Sheets page, plus the new multi-subscriber `engine.on(...)` event bus — and added an ASCII composition diagram (both surfaces → PlayerControlsR → per-surface ChordFlowPlayback, showing the method calls down and the event subscriptions up, matching the doc's existing ASCII-box style); (2) added `player-controls-component` to the UI dumb-views box in the §7 layering diagram and noted the bus on playback-component; (3) added playback-component.js + player-controls-component.js to the §2 wwwroot listing; (4) corrected the now-stale ScoreR "player-kind (metronome/count-in/soundfont) applied locally" clause in §5 to say that transport moved to PlayerControlsR, ScoreR keeping only the display-only staff-profile + content-kind options. Satisfies IN7 (architecture-ref sync in the same unit of work).
